<Application x:Class="EduGuardAgent.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EduGuardAgent"
             xmlns:behaviors="clr-namespace:EduGuardAgent.Behaviors"
             ShutdownMode="OnExplicitShutdown"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1E3A8A" />
        <SolidColorBrush x:Key="AccentBrush" Color="#EEF4FF" />
        <SolidColorBrush x:Key="PageBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextBrush" Color="#1F2A44" />
        <SolidColorBrush x:Key="MutedBrush" Color="#64748B" />
        <SolidColorBrush x:Key="OnlineBrush" Color="#2563EB" />
        <SolidColorBrush x:Key="SupervisionPendingBrush" Color="#D97706" />
        <SolidColorBrush x:Key="WarningBrush" Color="#DC2626" />
        <SolidColorBrush x:Key="SkyBorderBrush" Color="#C7D8F4" />
        <SolidColorBrush x:Key="SkySoftBrush" Color="#F5F8FF" />
        <SolidColorBrush x:Key="OnPrimaryBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="OnPrimaryMutedBrush" Color="#D7E7FF" />
        <SolidColorBrush x:Key="GlassCardBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="GlassBorderBrush" Color="#CAD7EA" />
        <SolidColorBrush x:Key="PageAtmosphereBrush" Color="#120EA5E9" />
        <SolidColorBrush x:Key="GlassDividerBrush" Color="#D7E2F2" />
        <SolidColorBrush x:Key="GlassProgressTrackBrush" Color="#D7E2F2" />
        <SolidColorBrush x:Key="HomeHeroTintBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="HomeHeroAccentBrush" Color="#FF38BDF8" />
        <SolidColorBrush x:Key="HomeSurfaceBrush" Color="#FFF5F8FF" />
        <SolidColorBrush x:Key="HomeSurfaceBorderBrush" Color="#FFD8E7FF" />

        <!-- Fixed palette for mode pickers â€” not swapped by ThemeService per supervision mode -->
        <SolidColorBrush x:Key="SegmentTrackBrush" Color="#EEF4FF" />
        <SolidColorBrush x:Key="SegmentBorderBrush" Color="#D8E7FF" />
        <SolidColorBrush x:Key="SegmentInactiveBrush" Color="#F7FAFF" />
        <SolidColorBrush x:Key="SegmentInactiveForegroundBrush" Color="#334155" />
        <SolidColorBrush x:Key="SegmentActiveBrush" Color="#2563EB" />
        <SolidColorBrush x:Key="SegmentActiveForegroundBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SegmentPastBrush" Color="#E8F2FF" />

        <LinearGradientBrush x:Key="PageGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1B1724" Offset="0" />
            <GradientStop Color="#10141C" Offset="1" />
        </LinearGradientBrush>

        <DropShadowEffect x:Key="GlassCardShadow"
                          Color="#000000"
                          BlurRadius="14"
                          ShadowDepth="0"
                          Opacity="0.08" />

        <DropShadowEffect x:Key="WindowShellShadow"
                          Color="#000000"
                          BlurRadius="48"
                          ShadowDepth="0"
                          Opacity="0.38" />

        <LinearGradientBrush x:Key="HeroGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0EA5E9" Offset="0" />
            <GradientStop Color="#F97316" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="LockOverlayGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0EA5E9" Offset="0" />
            <GradientStop Color="#F97316" Offset="1" />
        </LinearGradientBrush>

        <FontFamily x:Key="AppFontFamily">Aptos, Segoe UI, sans-serif</FontFamily>
        <FontFamily x:Key="AppEyebrowFontFamily">Aptos, Segoe UI, sans-serif</FontFamily>
        <CornerRadius x:Key="WindowCornerRadius">24</CornerRadius>
        <CornerRadius x:Key="CardCornerRadius">28</CornerRadius>
        <CornerRadius x:Key="SmallCornerRadius">18</CornerRadius>
        <CornerRadius x:Key="PillCornerRadius">999</CornerRadius>

        <DropShadowEffect x:Key="StatusGlow" Color="#2563EB" BlurRadius="9" ShadowDepth="0" Opacity="0" />
        <DropShadowEffect x:Key="ProgressGlow" Color="#2563EB" BlurRadius="10" ShadowDepth="0" Opacity="0" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource SmallCornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource OnPrimaryBrush}" />
            <Setter Property="Padding" Value="24,14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="LockDismissButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource CardBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryDarkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SkyBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource HomeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryDarkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource HomeSurfaceBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,10" />
        </Style>

        <Style x:Key="SubtleButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource HomeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryDarkBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource HomeSurfaceBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="RestrictionCardButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RestrictionTileButton" TargetType="Button" BasedOn="{StaticResource RestrictionCardButton}">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="Height" Value="112" />
            <Setter Property="MinHeight" Value="112" />
            <Setter Property="MaxHeight" Value="112" />
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style x:Key="SettingsRowButton" TargetType="Button" BasedOn="{StaticResource RestrictionCardButton}">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Height" Value="72" />
            <Setter Property="MinHeight" Value="72" />
            <Setter Property="MaxHeight" Value="72" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HomeSurfaceBrush}" />
                    <Setter Property="Opacity" Value="1" />

                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HomeSurfaceBorderBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource GlassCardBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{DynamicResource CardCornerRadius}" />
            <Setter Property="Effect" Value="{StaticResource GlassCardShadow}" />
            <Setter Property="CacheMode">
                <Setter.Value>
                    <BitmapCache EnableClearType="True" SnapsToDevicePixels="True" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassCardCompact" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="CornerRadius" Value="{DynamicResource SmallCornerRadius}" />
        </Style>

        <Style x:Key="HeroCard" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Padding" Value="30" />
            <Setter Property="BorderBrush" Value="{DynamicResource HomeHeroAccentBrush}" />

        </Style>

        <Style x:Key="MetricPill" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource HomeSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource HomeSurfaceBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="MinHeight" Value="54" />
        </Style>

        <Style x:Key="GuardiScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="VerticalScrollBarVisibility" Value="Hidden" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="PanningMode" Value="VerticalOnly" />
            <Setter Property="PanningDeceleration" Value="0.0015" />
            <Setter Property="PanningRatio" Value="1.0" />
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
            <Setter Property="behaviors:SmoothScroll.Enabled" Value="True" />
        </Style>

        <Style x:Key="LocalSectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style x:Key="LocalSectionBox" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}" />
            <Setter Property="CornerRadius" Value="{DynamicResource SmallCornerRadius}" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="BorderBrush" Value="{DynamicResource SkyBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>

        <Style x:Key="LocalModeSegmentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource SegmentInactiveBrush}" />
            <Setter Property="Foreground" Value="{StaticResource SegmentInactiveForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SegmentBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource SmallCornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SegmentPastBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="46" Height="26" SnapsToDevicePixels="True">
                            <Border x:Name="Track"
                                    Background="{DynamicResource GlassBorderBrush}"
                                    CornerRadius="13" />
                            <Border x:Name="Thumb"
                                    Width="22"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="White"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0"
                                    VerticalAlignment="Center">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.18" />
                                </Border.Effect>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource PrimaryBrush}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="BorderBrush" Value="{StaticResource SkyBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource SmallCornerRadius}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GuardiProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="{DynamicResource GlassProgressTrackBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="Effect" Value="{DynamicResource ProgressGlow}" />
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <local:InverseBoolToVisibilityConverter x:Key="BoolToVisibilityInverse" />
        <local:InverseBoolConverter x:Key="InverseBool" />
        <local:CountToVisibilityConverter x:Key="CountToVisibility" />
        <local:LevelStepBackgroundConverter x:Key="LevelStepBackgroundConverter" />
        <local:LevelStepForegroundConverter x:Key="LevelStepForegroundConverter" />
        <local:MatureRestrictionIconFontSizeConverter x:Key="MatureRestrictionIconFontSizeConverter" />
        <local:MatureRestrictionIconWeightConverter x:Key="MatureRestrictionIconWeightConverter" />
        <local:RestrictionTileWidthConverter x:Key="RestrictionTileWidthConverter" />
        <local:SlugEqualsMultiConverter x:Key="SlugEqualsConverter" />
        <local:InitialLetterConverter x:Key="InitialLetterConverter" />
        <local:IconTypeToVisibilityConverter x:Key="IconTypeToVisibilityConverter" />
    </Application.Resources>
</Application>

