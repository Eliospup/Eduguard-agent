using System.Windows.Controls;

namespace EduGuardAgent.Views.LocalSections;

/// <summary>Image-shield section of the local settings detail pane. DataContext = MainViewModel (inherited).</summary>
public partial class ImageShieldSection : UserControl
{
    public ImageShieldSection() => InitializeComponent();
}
